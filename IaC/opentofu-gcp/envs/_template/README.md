# OpenTofu template — Google Cloud (GCP)

Provisions a VPC, subnet, firewall rules, and Compute Engine instance pools
(App / Platform / Extras) using the `hashicorp/google` provider (~> 5.40).

Rendered per stack by the OpenSible backend into
`IaC/opentofu-gcp/envs/<stack-name>/` at plan/apply time.
