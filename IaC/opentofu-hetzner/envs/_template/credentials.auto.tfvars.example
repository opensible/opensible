# Copy to credentials.auto.tfvars (chmod 600) and paste your Hetzner Cloud API token.
# The token is created in Hetzner Cloud Console → Project → Security → API Tokens.
# Requires Read & Write scope.
hcloud_token = "REPLACE_ME_HETZNER_API_TOKEN"
